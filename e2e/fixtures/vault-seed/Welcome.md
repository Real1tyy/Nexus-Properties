# Welcome

This is the seed vault used by Nexus-Properties end-to-end tests. Specs seed
their own notes per run; this file exists so the seed directory is non-empty
and Obsidian opens it as a valid vault.
